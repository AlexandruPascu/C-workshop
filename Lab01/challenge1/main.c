#include <stdio.h>

int main()
{
    float x;
    // TODO 1: Citeste variabila x 

    while (x >= 0) {
        if (x - (int)x == 0) {
            // TODO 2: Printeaza la stdout numarul intreg
        } else {
            // TODO 3: Printeaza la stderr numarul real cu precizie de doua zecimale
        }
        // TODO 4: Citeste urmatoarea variabila
    }

    return 0;
}
